# Instalación

Desde la raíz del repositorio ejecuta `./install.sh`. El proceso guarda primero WezTerm y `.bashrc` en `~/.config/robotics-dev-environment/backups/<timestamp>`, conserva la configuración anterior y crea el enlace de WezTerm. Bash requiere revisión manual.
